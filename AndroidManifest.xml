<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pl.devcrowd.app"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:name="pl.devcrowd.app.acra.CrashReports" >
        
        <receiver
            android:name="pl.devcrowd.app.broadcasts.AlarmReceiver"
            android:enabled="true" >
            <intent-filter>
                <action android:name="AlarmReceiver" />
            </intent-filter>
        </receiver>

        <activity
            android:name="pl.devcrowd.app.activities.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="pl.devcrowd.app.activities.ScheduleDetailsActivity"
            android:parentActivityName="pl.devcrowd.app.activities.MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="pl.devcrowd.app.activities.MainActivity" />
        </activity>

        <service
            android:name=".services.ApiService"
            android:enabled="true"
            android:exported="false" >
            <intent-filter>
                <action android:name="pl.devcrowd.app.ACTION_GET_PRESENTATIONS" />
                <action android:name="pl.devcrowd.app.ACTION_GET_SPEAKERS" />
            </intent-filter>
        </service>

        <provider
            android:name=".db.DevcrowdContentProvider"
            android:authorities="pl.devcrowd.app.db"
            android:exported="false" >
        </provider>
    </application>

</manifest>